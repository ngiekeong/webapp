on: [push]

jobs:
  zap_scan:
      runs-on: ubuntu-latest
      name: Scan the web application
      steps:
       - name: Zap Scan
         uses: zaproxy/action-baseline@v0.4.0
         with:
          target: 'http:54.173.108.164:8080/webapp/'
          
